let x = "value1";
let y = "value2";

function multiplyfn(x, y) {
return(x * y);
}

 alert(inputVal);

function dividefn(x, y) {
return(x / y);
}

if (times)
console.log(times);

if (division)
console.log(division);
